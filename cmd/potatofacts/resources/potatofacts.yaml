server:
  request-logging:
    enabled: true
    block-list:
      - /health/readiness
      - /health/liveness
  http:
    host: 0.0.0.0
    port: 9001
    ssl.enabled: false
    prefix: /api
  spa:
    enabled: true
    prefix: /ui